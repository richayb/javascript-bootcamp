var output = '#'; //Set character to be outputted

for (var i = 0; i < 7; i++){ //Loop and count to 7
    console.log(output); //Output the single row of character(s)
    output += '#'; //Add a character
}